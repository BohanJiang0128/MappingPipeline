# DensePose CSE Mapping Pipeline – Python dependencies
#
# Install:   pip install -r requirements.txt
#
# Notes:
#   - PyTorch + CUDA must be installed separately for your GPU/driver version.
#     See https://pytorch.org/get-started/locally/
#   - Detectron2 must be installed separately.
#     See https://detectron2.readthedocs.io/en/latest/tutorials/install.html
#   - DensePose is included as a local directory (densepose/) — no pip install needed.
#   - The Hugging Face diffusers model is downloaded on first run.

# Core
numpy
opencv-python
Pillow
tqdm

# Diffusion outpainting (Step 1)
diffusers
transformers
accelerate
safetensors

# QA selection web interface (Step 2)
flask

# DensePose (Steps 4–5) – detectron2 & torch installed separately;
# the densepose/ package is local and does not need to be installed.

# BSA computation (Step 6) + Navigation tool
pyvista

# Navigation tool (navigation_tool.py)
PySide6
pyvistaqt
